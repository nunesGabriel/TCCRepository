<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Teste de botão padrão
    <style name="ButtonStyle" parent="android:Theme">
        <item name="android:buttonStyle">@style/OptButton</item>
    </style>

    <style name="OptButton" parent="android:style/Widget.Button">
        <item name="android:textSize">19dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:background">@drawable/button_rsp</item>
    </style>-->

</resources>
